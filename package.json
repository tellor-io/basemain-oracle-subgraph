{
  "name": "tellor-oracle-base",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tellor-oracle-base",
    "create-local": "graph create --node http://localhost:8020/ tellor-oracle-base",
    "remove-local": "graph remove --node http://localhost:8020/ tellor-oracle-base",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tellor-oracle-base",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
